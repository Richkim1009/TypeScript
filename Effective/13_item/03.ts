interface IState {
  name: string;
  capital: string;
}
interface IState {
  population: number;
}
const wyoming2: IState = {
  name: 'Wyoming',
  capital: 'Cheyenne',
  population: 500_000
};

// 인터페이스는 보강 기능이 있다.

